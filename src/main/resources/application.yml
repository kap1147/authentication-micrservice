server:
  port: 8080

spring:
  profiles:
    active: "default"
  application:
    name: auth-microservice
  data:
    mongodb:
      uri: mongodb://${DB_HOST}:${DB_PORT}/${DB_NAME}?authMechanism=SCRAM-SHA-256

logging:
  level:
    root: DEBUG
    org:
      springframework: DEBUG
      mongodb: DEBUG

SECRET_KEY: ${SECRET_KEY}